---
title: Overview
description: Inspect queries, mutations, cache and optimistic updates in real time — directly in your IDE or inside your running app.
---

Qora ships with first-class developer tooling across two independent surfaces. Both share the same event protocol and work without any configuration changes to your production code.

::card-group
  ::card
  ---
  title: In-App Overlay
  icon: i-heroicons-squares-2x2
  to: /devtools/overlay
  ---
  A draggable panel that lives inside your running app — inspired by TanStack Query's overlay. Zero overhead in release builds.
  ::

  ::card
  ---
  title: IDE Extension
  icon: i-heroicons-command-line
  to: /devtools/ide-extension
  ---
  A native tab inside Flutter DevTools. Works in VS Code and IntelliJ. Inspect the full cache and send commands directly from your IDE.
  ::
::

---

## Comparison

| | In-App Overlay | IDE Extension |
|---|---|---|
| **Setup** | Wrap your app with `QoraInspector` | Add `qora_devtools_extension` to dependencies |
| **Where it runs** | Inside your app | Inside Flutter DevTools (VS Code / IntelliJ) |
| **Transport** | Direct — same process | Dart VM Service (WebSocket) |
| **Mutations timeline** | ✅ | ✅ |
| **Cache inspector** | ✅ | ✅ |
| **Optimistic updates** | ✅ | ✅ |
| **Refetch / Invalidate** | ✅ | ✅ |
| **Widget Tree** | — | ✅ |
| **Data Diff** | — | ✅ |
| **Production overhead** | None (`kDebugMode` guard) | None (stripped by tree-shaker) |
| **Works on physical device** | ✅ | ✅ (via USB) |

---

## How it works

Both surfaces listen to the same stream of events produced by `QoraTracker` — an interface implemented by your `QoraClient`. In production, you omit the tracker and `QoraClient` defaults to `NoOpTracker`, which means **zero overhead, zero dead code in your release bundle**.

```
QoraClient
    │
    ├── NoOpTracker (production)     → silent, inlined away by the compiler
    ├── VmTracker  (IDE extension)   → pushes events over Dart VM Service
    └── OverlayTracker (overlay)     → pushes events directly in-memory
```

You pass the tracker explicitly when constructing `QoraClient`. In production you omit it — the default `NoOpTracker` is used automatically.

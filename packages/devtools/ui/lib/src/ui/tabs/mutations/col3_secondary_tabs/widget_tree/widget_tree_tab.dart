import 'package:flutter/material.dart';

/// Placeholder tab for widget tree integration.
class WidgetTreeTab extends StatelessWidget {
  /// Creates widget tree tab.
  const WidgetTreeTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Widget Tree integration is coming soon.'));
  }
}

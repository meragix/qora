import 'package:flutter/material.dart';

/// Placeholder tab for payload/data diff visualization.
class DataDiffTab extends StatelessWidget {
  /// Creates data diff tab.
  const DataDiffTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Data Diff panel is coming soon.'));
  }
}

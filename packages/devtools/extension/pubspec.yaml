name: qora_devtools_extension
description: Runtime VM service bridge that exposes Qora state to DevTools.
version: 0.1.0
homepage: https://github.com/meragix/qora
repository: https://github.com/meragix/qora
issue_tracker: https://github.com/meragix/qora/issues
publish_to: none

environment:
  sdk: '>=3.5.0 <4.0.0'

resolution: workspace

dependencies:
  qora:
    path: ../../dart
  qora_devtools_shared:
    path: ../shared

dev_dependencies:
  test: ^1.26.0
  flutter_lints: ^6.0.0

name: qora_devtools_shared
description: Shared protocol contracts, models, and codecs for Qora DevTools.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.5.0 <4.0.0"

resolution: workspace

dependencies: {}

dev_dependencies:
  test: ^1.26.0
  flutter_lints: ^6.0.0

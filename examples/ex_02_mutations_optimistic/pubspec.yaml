name: ex_02_mutations_optimistic
description: "Optimistic mutations with setQueryData and automatic rollback via restoreQueryData."
publish_to: 'none'

version: 1.0.0

environment:
  sdk: ^3.10.8

dependencies:
  flutter:
    sdk: flutter

  # Qora packages (local path for monorepo)
  qora_flutter:
    path: ../../packages/flutter
  qora_devtools_overlay:
    path: ../../packages/devtools/overlay
  qora_devtools_extension: 
    path: ../../packages/devtools/extension

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

name: ex_07_complete_todo_app
description: "Production-ready todo app: auth flow, Hive persistence, offline-first, optimistic updates, infinite scroll, and clean architecture."
publish_to: 'none'
version: 1.0.0

environment:
  sdk: ^3.10.8

dependencies:
  flutter:
    sdk: flutter

  http: ^1.2.0
  hive_flutter: ^1.1.0
  qora_flutter:
    path: ../../packages/flutter
  qora_devtools_overlay:
    path: ../../packages/devtools/overlay
  qora_devtools_extension:
    path: ../../packages/devtools/extension

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  
flutter:
  uses-material-design: true

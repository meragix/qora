name: ex_04_offline_first
description: "Offline-first todo app: PersistQoraClient, OfflineMutationQueue, and NetworkMode.offlineFirst."
publish_to: 'none'

version: 1.0.0

environment:
  sdk: ^3.10.8

dependencies:
  flutter:
    sdk: flutter

  # Qora packages (local path for monorepo)
  qora_flutter:
    path: ../../packages/flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

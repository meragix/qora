name: ex_03_infinite_scroll
description: "Infinite scroll feed with cursor pagination, maxPages windowing, and optimistic post creation."
publish_to: 'none'

version: 1.0.0

environment:
  sdk: ^3.10.8

dependencies:
  flutter:
    sdk: flutter

  # Qora packages (local path for monorepo)
  qora_flutter:
    path: ../../packages/flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

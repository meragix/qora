name: ex_05_network_aware
description: "Network-aware queries: NetworkMode, FetchStatus.paused, OfflineMutationQueue, and ReconnectStrategy."
publish_to: 'none'

version: 1.0.0

environment:
  sdk: ^3.10.8

dependencies:
  flutter:
    sdk: flutter

  # HTTP client
  http: ^1.2.0

  # Qora packages (local path for monorepo)
  qora_flutter:
    path: ../../packages/flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

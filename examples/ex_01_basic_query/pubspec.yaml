name: ex_01_basic_query
description: "Learn how to fetch and display data with Qora's basic query features."
publish_to: 'none'

version: 1.0.0

environment:
  sdk: ^3.10.8

dependencies:
  flutter:
    sdk: flutter

  # Qora packages (local path for monorepo)
  qora_flutter:
    path: ../../packages/flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
